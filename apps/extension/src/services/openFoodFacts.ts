/**
 * Open Food Facts client (stub).
 * Network calls live here — not in content scripts or the allergen engine.
 */

export async function lookupProductByBarcode(barcode: string): Promise<null> {
  void barcode;
  return null;
}
